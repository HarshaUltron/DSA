# DSA
This repo is for my DSA notes and codes

git add "Time and Space Complexity"

git commit -m "Initial DSA upload"

git branch -M main

git config --global user.email "you@example.com"

git config --global user.name "Your Name"

git push -u origin main

type : to enter any command

git pull origin main --allow-unrelated-histories [for the first time if you have created readme in repo in github, there might be discrepancy in your local and global, so execute this cmd]


